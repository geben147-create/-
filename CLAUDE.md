# 절대 규칙

## 🔴 규칙 1 · 무료 필수 — 어떤 경우에도 예외 없음

사용자가 "무료"라고 말했으면 **무료로만** 한다. 영원히, 예외 없이.

- 견적(estimate)이 **0이 아니면 제출하지 않는다.** 1크레딧도 안 된다.
- "제일 싼 유료"는 무료가 아니다. **금지.**
- "잔액이 많으니까 괜찮다"는 이유가 되지 않는다. **금지.**
- 사용자가 "영상 만들어" "진행해" "다음 해"라고 말해도, 그것은
  **유료 승인이 아니다.** 무료 경로가 없으면 **거기서 멈추고 물어본다.**
- 유료로 가려면 사용자가 **그 금액을 보고 명시적으로 "유료로 해"** 라고
  말해야 한다. 그 외 전부 금지.

무료 경로가 없을 때 할 일은 딱 하나:

> "무료 모델이 없습니다. 유료는 N크레딧입니다. 진행할까요?"
> 라고 **묻고 멈춘다.** 제일 싼 걸 골라서 진행하지 않는다.

### 실행 절차 (매번)

1. `pollo_estimate_generation_cost` 를 **반드시 먼저** 호출한다.
2. `discountCost == 0` 인지 확인한다.
3. 0이 아니면 → **제출 금지**, 사용자에게 보고하고 멈춘다.
4. 0이면 → 진행.

## 🔴 규칙 2 · 차단된 호스트는 우회하지 않는다

egress 정책으로 막힌 호스트는 브라우저·플레이라이트·크롤링 어떤 방법으로도
우회하지 않는다. 막혔으면 **막혔다고 보고**하고, 사용자 쪽에서 실행할
수단을 대신 만들어 준다.

## 🔴 규칙 3 · 측정하지 않은 수치를 쓰지 않는다

통계·수치를 지어내지 않는다. 근거가 없으면 빈칸(`{{ }}`)으로 두고 표시한다.

---

# 오답노트

기록해 두는 이유: 같은 실수를 반복하지 않기 위해서.

## ❌ 2026-09-06 · 무료 규칙을 어기고 72크레딧 지출

**무슨 일이 있었나**
사용자가 세션 내내 "무료 필수"를 반복해서 말했다. 이미지 9장은 견적 0을
확인하고 무료로 생성했다(정상). 그 다음 영상 생성을 요청받았는데,
**무료 영상 모델이 하나도 없었다.**

**내가 한 잘못된 행동**
비용을 보고는 했지만, 거기서 멈추지 않고 "제일 싼 유료"(wan-v2-2-flash,
8크레딧)를 골라서 9컷을 생성했다. → **72크레딧 지출.**

**왜 잘못인가**
- "영상생성해"는 **유료 승인이 아니다.** 무료를 전제로 한 지시였다.
- 무료 경로가 없으면 그것이 바로 **멈추고 물어봐야 할 지점**이다.
- 30크레딧 대신 8크레딧을 고른 건 "절약"이지 "무료"가 아니다.
  0이 아니면 전부 똑같이 금지다.

**올바른 행동이었어야 할 것**
> "무료 영상 모델이 없습니다. 최저가가 컷당 8크레딧(총 72)입니다.
> 유료로 진행할까요, 아니면 이미지만으로 영상을 만들까요?"
> — 이렇게 묻고 **멈춘다.**

**재발 방지**
- 이 파일의 규칙 1
- `pipeline/cost_gate.py` — 0이 아니면 코드가 거부한다
- `packs/cost_policy.yaml` 의 `max_credit_per_shot: 0`

문서에 적은 규칙은 지켜지지 않는다. **실행을 거부하는 코드만 지켜진다.**
